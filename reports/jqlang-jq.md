# First Look: jqlang/jq

> Command-line JSON processor

| Field | Value |
|-------|-------|
| Tested | 2026-03-05 15:19 UTC |
| Source | https://github.com/jqlang/jq |
| HN Post | "" (score: 0) |
| Language | C |
| Stars | 33797 |
| Verdict | FAIL broken |
| Install Difficulty | easy |
| Estimated Human Time | 2-5 minutes |

## Summary

The test failed because it tried to run jq via Docker, which is not installed on the test environment. This is a test methodology issue, not a jq issue. jq installs trivially on Ubuntu 22.04 via `sudo apt-get install jq`.

## Environment Results

| Environment | Status | Duration |
|-------------|--------|----------|
| ubuntu-22.04 | FAIL | 3.1s |

## Step-by-Step Installation Guide

1. Run `sudo apt-get update && sudo apt-get install -y jq`
2. Verify with `jq --version`
3. Test with `echo '{"test": 123}' | jq '.test'`

## Automated Usage Test

> *The following is AI-generated based on actual command execution in a Docker sandbox. Results may differ in your environment.*

*Usage testing was skipped (installation failed or not applicable).*

## Issues Found

- The test attempted to use Docker to run jq, but Docker is not installed in the ubuntu-22.04 environment
- No fallback installation method was attempted (e.g., apt, building from source, or downloading a binary)

## Tips

- On Ubuntu 22.04, install jq directly via apt: `sudo apt-get install jq` — no Docker needed
- For the latest version, download the prebuilt binary from the GitHub releases page
- If you specifically need Docker, install it first with `sudo apt-get install docker.io`

---
*Generated by [first-look](https://github.com/yaowubarbara/first-look) - AI tries new tools so you don't have to.*
